
#include "DrawCard.h"

namespace GCB::GameEngine {
    void DrawCard::execute(State::GameState& state, 
        const std::vector<Core::GameValue>& args) {
        if (args.size() < 2)
            return;
    }
}